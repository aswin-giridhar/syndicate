[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
# submitReceipt carries more locals than the legacy codegen can hold on stack
# once quota-share settlement and the ERC-8004 write-through were added.
via_ir = true
